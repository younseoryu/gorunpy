# GoRunPy Example

```bash
pip install -e ../python[build]

make build
make generate
make run
```

Output:
```
1 + 2 = 3
3.5 * 2.0 = 7.0
Hello, World!
mean([1,2,3,4,5]) = 3.0
Error: division by zero
```
